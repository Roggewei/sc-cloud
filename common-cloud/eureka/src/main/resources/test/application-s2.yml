
server:
  #注册中心端口号
  port: 8002

eureka:
  instance:
    hostname: peer2
  ##false表明自己是一个server
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://peer1:8001/eureka/
    #服务名称
    spring:
      application:
        name:eureka-server